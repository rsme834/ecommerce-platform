{
  "scripts": {
    "db:seed": "tsx prisma/seed.ts",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:reset": "prisma migrate reset",
    "db:studio": "prisma studio",
    "db:pull": "prisma db pull",
    "db:validate": "prisma validate",
    "db:clear": "tsx scripts/db-utils.ts clear",
    "db:stats": "tsx scripts/db-utils.ts stats",
    "db:backup": "tsx scripts/db-utils.ts backup",
    "db:restore": "tsx scripts/db-utils.ts restore",
    "db:performance": "tsx scripts/db-utils.ts performance",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:build": "docker-compose up --build -d",
    "docker:clean": "docker-compose down -v && docker system prune -f",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:fix": "next lint --fix",
    "analyze": "cross-env ANALYZE=true next build",
    "check-updates": "npm-check-updates",
    "security-audit": "npm audit",
    "postinstall": "prisma generate",
    "precommit": "lint-staged"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "prisma": "^6.12.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
